# TODO_ADMIN_CODE.md

This file tracks progress for AdminDashboard WhatsApp number manager changes.

## Steps
- [ ] Add Firebase imports to `src/pages/admin/AdminDashboard.jsx`
- [ ] Add WhatsApp state variables after `savingGiveaway` state
- [ ] Add `loadWhatsapp()` call inside the main `useEffect` before cleanup return
- [ ] Insert the WhatsApp Number Manager UI block before `{/* Recent Winners */}`
- [ ] Sanity check: ensure file still compiles (no unused vars / syntax issues)

