export const SOCIAL_PLATFORMS = ['tiktok', 'telegram'];

// These are admin-configurable in a real backend.
// For now, we keep a simple in-memory config so the UI flow works.
export const defaultSocialLinks = {
    tiktok: '',
    telegram: ''
};

